interface:
  display_name: "InkLoom Developer"
  short_description: "Build and visually validate InkLoom explainers"
  default_prompt: "Use $inkloom-dev to build and validate a node-local Remotion explainer, time each scene by content density, catalog each new visual direction under a style name through its original MDX route, and publish its scene media without creating a duplicate demo node."
policy:
  allow_implicit_invocation: true
