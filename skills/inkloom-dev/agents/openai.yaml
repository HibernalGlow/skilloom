interface:
  display_name: "InkLoom Developer"
  short_description: "Publish independent InkLoom Remotion explainers"
  default_prompt: "Use $inkloom-dev to create two independent Remotion explainers from a completed legal Markdown note, keep that note untouched, and publish each through a thin InkLoom MDX carrier."
policy:
  allow_implicit_invocation: true
