interface:
  display_name: "InkLoom Developer"
  short_description: "Build and visually validate InkLoom explainers"
  default_prompt: "Use $inkloom-dev to publish Remotion explainers from a completed legal note, preserve the original full-length video and pagination contract, require diagrammatic visual explanation and full page-still QA, and additionally run pnpm animation:publish-avif to publish one once-playing q45/CRF35 2560x1440 AVIF per existing semantic scene with website copy controls and SiYuan playback support."
policy:
  allow_implicit_invocation: true
