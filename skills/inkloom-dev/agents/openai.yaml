interface:
  display_name: "InkLoom Developer"
  short_description: "Publish InkLoom explainers and SiYuan embeds"
  default_prompt: "Use $inkloom-dev to publish two Remotion explainers from a completed legal note and, when explicitly requested, use $siyuan-cli to insert their production URLs into the correct positions of an existing SiYuan note."
policy:
  allow_implicit_invocation: true
