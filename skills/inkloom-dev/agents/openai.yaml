interface:
  display_name: "InkLoom Developer"
  short_description: "Build and visually validate InkLoom explainers"
  default_prompt: "Use $inkloom-dev to build a diagrammatic Remotion explainer with a node-local visual direction, preserve the full-length video and pagination contract, run pnpm animation:styles and full page-still QA, and publish one once-playing q45/CRF35 2560x1440 AVIF per semantic scene with website and SiYuan playback support."
policy:
  allow_implicit_invocation: true
